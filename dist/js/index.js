/*

window.addEventListener('scroll', function(){
    let scrollPromoter = document.getElementById('ScrollPromoter');
    let scrollY = window.pageYOffset;
    if (scrollY > 50) {
        scrollPromoter.style.opacity = 0;
    }else {
        scrollPromoter.style.opacity = 1;
    }
}, false)

*/
